import { Button } from '@/components/ui/button'

const Home = () => {
    return (
        <main className='flex min-h-screen flex-col items-center justify-center px-6 py-24'>
            <Button>Amster</Button>
        </main>
    )
}

export default Home
